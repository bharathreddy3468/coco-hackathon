---
created: 2026-08-06T03:17:37.221Z
session: d1cd127b-22bc-44c9-a905-6e1375d17613
working_directory: C:\Temp\coco-hackathon\backend
---

## Snowflake Migration - Implementation Plan (Confirmed)

Replace SQLAlchemy/SQLite entirely with Snowflake Python Connector. 8 implementation steps in dependency order:

1. **s6e22** - Update `pyproject.toml` (remove sqlalchemy/aiosqlite, add snowflake-connector-python) + `settings.py` (Snowflake config fields)
2. **s061b** - Rewrite `app/database/session.py` (Snowflake connection factory) + create `snowflake_init.py` (DDL for 4 tables) + update `__init__.py`
3. **s42b7** - Convert `app/models/claim.py` from SQLAlchemy ORM to Pydantic models
4. **s527f** - Create `app/database/claim_repository.py` (Snowflake CRUD layer)
5. **s6f25** - Create `app/workflow/context.py` (WorkflowContext dataclass) + `app/services/state_manager.py` (StateManager)
6. **sad61** - Rewrite `app/workflow/engine.py` (use StateManager + repository)
7. **s740c** - Rewrite `app/services/claim_service.py` (use repository + state_manager)
8. **s345b** - Update `app/api/v1/endpoints/claims.py` (remove db dependency, add /resume) + `main.py` (init Snowflake tables)

Verification step **s1464** runs after all implementation.

4 implementors will drain the sequential dependency chain. 1 verifier runs after.